–азработать jUnit тесты, дл€ проверки работы модулей из первого задани€.

»спользовал Maven 3.2.3, jUnit 4.11, jdk 8 Update 25, Eclipse Luna.

—амое легкое задание. «апускать только через IDE.
—делал тесты только дл€ одного модул€, и так все пон€тно.

папка common содержит проект с интерфейсом StringConvertable, который имплементируют модули.
папка firstModule содержит проект первого модул€.

convert()  возвращает строку, где удалено каждое второе слово. ѕри этом не важно сколько было разделителей.
разделители - пробелы,знаки препинани€ и т.д.

convert("мама     мыла      раму") = "мама           раму"

getDeletedStrings()  возвращает ArrayList с удаленными словами.

getDeletedStrings("мама     мыла      раму") вернет ArrayList со словом "мыла".

 







